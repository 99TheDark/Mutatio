{
    "main": "server/server.js",
    "scripts": {
        "start": "node server/server && npx kill-port 8080"
    },
    "dependencies": {
        "express": "^4.18.2",
        "kill-port": "^2.0.1",
        "lemmatizer": "^0.0.1",
        "socket.io": "^4.6.1"
    }
}